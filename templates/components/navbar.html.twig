<nav id="main-navbar" class="bg-base-100 shadow-lg sticky top-0 z-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16 items-center">
            <!-- Logo / Brand -->
            <div class="flex-shrink-0">
                <a href="{{ path('app_home') }}" class="text-2xl font-bold text-primary">MonBlog</a>
            </div>

            <!-- Menu Desktop -->
            <div class="hidden md:flex space-x-4">
                <a href="{{ path('app_user_members') }}" class="btn btn-ghost btn-sm">Utilisateurs</a>
                <a href="{{ path('app_categories') }}" class="btn btn-ghost btn-sm">Catégories</a>
                <a href="{{ path('app_articles') }}" class="btn btn-ghost btn-sm">Articles</a>
            </div>

            <!-- Menu Mobile -->
            <div class="md:hidden">
                <div class="dropdown dropdown-end">
                    <label tabindex="0" class="btn btn-ghost btn-circle">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
                        </svg>
                    </label>
                    <ul tabindex="0" class="dropdown-content menu p-2 shadow bg-base-100 rounded-box w-52">
                        <li><a href="{{ path('app_user_members') }}">Utilisateurs</a></li>
                        <li><a href="{{ path('app_categories') }}">Catégories</a></li>
                        <li><a href="{{ path('app_articles') }}">Articles</a></li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</nav>
