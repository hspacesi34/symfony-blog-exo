{% extends 'base.html.twig' %}

{% block title %}Accueil - MonBlog{% endblock %}

{% block body %}
<div class="min-h-[calc(100vh-var(--navbar-height))] bg-base-200 flex flex-col items-center justify-center py-16 px-4 sm:px-6 lg:px-8">

    <!-- Header -->
    <header class="text-center mb-16">
        <h1 class="text-5xl font-extrabold text-primary mb-4">Bienvenue sur MonBlog</h1>
        <p class="text-lg text-gray-500 max-w-2xl mx-auto">Découvrez nos utilisateurs, catégories et articles.</p>
    </header>

    <!-- Cards principales -->
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8 w-full max-w-7xl">

        <!-- Utilisateurs -->
        <div class="card bg-base-100 shadow-xl hover:shadow-2xl transition-shadow duration-300 rounded-xl p-6 text-center">
            <div class="mb-4">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-16 w-16 mx-auto text-primary" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5.121 17.804A4.992 4.992 0 015 15a5 5 0 115 5h0a5 5 0 01-4.879-2.196zM15 11a4 4 0 10-8 0 4 4 0 008 0z" />
                </svg>
            </div>
            <h2 class="text-xl font-bold mb-2">Utilisateurs</h2>
            <p class="text-gray-500 mb-4">Voir tous les utilisateurs inscrits sur le blog.</p>
            <a href="{{ path('app_user_all') }}" class="btn btn-primary btn-sm">Accéder</a>
        </div>

        <!-- Catégories -->
        <div class="card bg-base-100 shadow-xl hover:shadow-2xl transition-shadow duration-300 rounded-xl p-6 text-center">
            <div class="mb-4">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-16 w-16 mx-auto text-primary" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
                </svg>
            </div>
            <h2 class="text-xl font-bold mb-2">Catégories</h2>
            <p class="text-gray-500 mb-4">Parcourez les différentes catégories d’articles du blog.</p>
            <a href="{{ path('app_category_all') }}" class="btn btn-primary btn-sm">Accéder</a>
        </div>

        <!-- Articles -->
        <div class="card bg-base-100 shadow-xl hover:shadow-2xl transition-shadow duration-300 rounded-xl p-6 text-center">
            <div class="mb-4">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-16 w-16 mx-auto text-primary" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21H5a2 2 0 01-2-2V5a2 2 0 012-2h9l7 7v9a2 2 0 01-2 2z" />
                </svg>
            </div>
            <h2 class="text-xl font-bold mb-2">Articles</h2>
            <p class="text-gray-500 mb-4">Consultez tous les articles publiés sur le blog.</p>
            <a href="{{ path('app_article_all') }}" class="btn btn-primary btn-sm">Accéder</a>
        </div>

    </div>
</div>
{% endblock %}
